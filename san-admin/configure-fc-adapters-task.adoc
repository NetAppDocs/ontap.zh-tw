---
permalink: san-admin/configure-fc-adapters-task.html 
sidebar: sidebar 
keywords: configure, fc, adapters, target, initiator, mode 
summary: 每個板載 FC 連接埠都可以個別設定為啟動器或目標。某些 FC 介面卡上的連接埠也可以像板載 FC 連接埠一樣,個別設定為目標連接埠或啟動器連接埠。 
---
= 設定FC介面卡
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
內建 FC 介面卡和某些 FC 擴充卡可以個別設定為啟動器或目標連接埠。其他 FC 擴充介面卡在原廠設定為啟動器或目標，無法變更。透過支援的 UTA2 卡（使用 FC SFP+ 介面卡設定），也可以使用額外的 FC 連接埠。

啟動器連接埠可用於直接連接至後端磁碟櫃，也可能是外部儲存陣列。目標連接埠只能用於連接 FC 交換器。

為 FC 設定的內建連接埠和 CNA/UTA2 連接埠數量會因控制器機型而異。支援的目標擴充介面卡也會因控制器機型而異。如需控制器機型內建 FC 連接埠和支援的目標擴充介面卡的完整清單，請參閱link:https://hwu.netapp.com["NetApp Hardware Universe"^]。



== 將FC介面卡設定為啟動器模式

啟動器模式用於將連接埠連接到磁帶機、磁帶庫或具有 link:https://docs.netapp.com/us-en/ontap-fli/get-started/learn-about-san-data-migration.html["外部 LUN 匯入（FLI）"^] 的第三方儲存設備。

.開始之前
* 介面卡上的LIF必須從其成員所在的任何連接埠集中移除。
* 在將實體連接埠的特性從目標變更為啟動器之前，必須遷移或銷毀使用要修改之實體連接埠的每個儲存虛擬機器（SVM）的所有 LIF。


[NOTE]
====
NVMe/FC 不支援啟動器模式。

====
.步驟
. 移除介面卡上的所有LIF：
+
[source, cli]
----
network interface delete -vserver _SVM_name_ -lif _lif_name_,_lif_name_
----
. 讓介面卡離線：
+
[source, cli]
----
network fcp adapter modify -node _node_name_ -adapter _adapter_port_ -status-admin down
----
+
如果介面卡未離線、您也可以從系統上適當的介面卡連接埠拔下纜線。

. 將介面卡從目標變更為啟動器：
+
[source, cli]
----
system hardware unified-connect modify -t initiator _adapter_port_
----
. 重新啟動裝載您所變更介面卡的節點。
. 驗證FC連接埠的組態設定是否正確：
+
[source, cli]
----
system hardware unified-connect show
----
. 將介面卡重新連線：
+
[source, cli]
----
node run -node _node_name_ storage enable adapter _adapter_port_
----




== 將FC介面卡設定為目標模式

目標模式用於將連接埠連接至FC啟動器。

同樣的步驟也用於設定 FC 傳輸協定和 FC-NVMe 傳輸協定的 FC 介面卡。不過、只有某些FC介面卡支援FC-NVMe。如需支援 FC-NVMe 傳輸協定的介面卡清單，請參閱link:https://hwu.netapp.com["NetApp Hardware Universe"^]。

.步驟
. 使介面卡離線：
+
[source, cli]
----
node run -node _node_name_ storage disable adapter _adapter_name_
----
+
如果介面卡未離線、您也可以從系統上適當的介面卡連接埠拔下纜線。

. 將介面卡從啟動器變更為目標：
+
[source, cli]
----
system node hardware unified-connect modify -t target -node _node_name_ adapter _adapter_name_
----
. 重新啟動裝載您所變更介面卡的節點。
. 驗證目標連接埠的組態是否正確：
+
[source, cli]
----
network fcp adapter show -node _node_name_
----
. 將介面卡上線：
+
[source, cli]
----
network fcp adapter modify -node _node_name_ -adapter _adapter_port_ -state up
----




== 設定 FC 介面卡以直接連接至 FC 和 FC-NVMe 主機

從 ONTAP 9.19.1 版本開始，無需 FC 交換器即可在 FC 和 FC-NVMe 主機與 ONTAP 儲存之間建立連線。主機可以直接連接到 AFF、ASA、ASA r2 和 FAS 系統上的 FC 適配器連接埠。在遠端站點和混合式環境中，這種直接連接功能尤其重要，因為在這些環境中，簡單性至關重要。必須先將 FC 連接埠離線並修改其拓撲結構，才能將其直接連接到主機。

直接連接組態中使用的 FC 連接埠有以下限制：

* 連接埠只能指派給一個儲存虛擬機器 （SVM）。
* 設定為直接連接後，只能將一個網路介面 (LIF) 指派給 port。
* 此連接埠可支援 FC 傳輸協定或 FC-NVMe 傳輸協定，但不能同時支援兩者。


.開始之前
您必須是叢集管理員。

[role="tabbed-block"]
====
.系統管理員
--
. 在 System Manager 中，選擇 *Network > FC ports*。
. 選擇要配置為直接連接的連接埠旁的 image:icon_kabob.gif["選擇圖示"]，然後選擇 *編輯拓撲*。
. 在 *Topology* 下，選擇 *Direct*，然後選擇 *I want to change the topology and take the port offline*。
. 選擇 *Save* 。
. 將目標連接埠的纜線直接連接至主機啟動器。
. 如果您使用的是 ASA r2 系統，請依照link:https://docs.netapp.com/us-en/asa-r2/administer/manage-client-vm-access.html#create-a-lif-network-interface["此 System Manager 程序"^]建立 LIF。如果您使用的是 AFF、ASA 或 FAS 系統，請依照下列步驟建立 LIF：
+
.. 選擇 *Network > Interfaces*，然後選擇 image:icon_add.gif["新增圖示"]。
.. 選擇 FC 或 NVMe/FC 傳輸協定。
.. 為 LIF 命名，或接受 ONTAP 產生的名稱。
.. 如果至少配置了一個子網路，則可以為 LIF 選擇一個子網路。
.. 如果未選擇子網路，則可以指定廣播網域的 IP 位址和子網路遮罩。
.. 從廣播網域中選擇主連接埠。
+
根據廣播網域或子網路選擇顯示主連接埠控制資訊。

.. 選擇*保存*。
+
建立 LIF 後，連接埠會自動上線。





.接下來呢？
在 System Manager 中、選取 * 網路 > FC 連接埠 * 、然後確認 * 拓撲 * 顯示為 * 直接 * 。

--
.CLI
--
. 請確認您要用於直接連接的連接埠支援直接拓撲：
+
[source, cli]
----
network fcp adapter show -fields topologies-supported
----
. 檢視用於介面卡連接埠的 LIF 管理和作業狀態：
+
[source, cli]
----
network interface show -vserver <SVM_name>
----
. 將用於介面卡連接埠的 LIF 設為離線：
+
[source, cli]
----
network interface modify -vserver <SVM_name> -lif <*---> -status-admin down
----
. 從介面卡 port 移除所有 LIF：
+
[source, cli]
----
network interface delete -vserver <SVM_name> -lif <lif_name>,<lif_name>
----
. 使介面卡離線：
+
[source, cli]
----
network fcp adapter modify -node <node_name> -adapter <adapter_port> -status-admin down
----
. 將介面卡連接埠拓撲變更為直接連接：
+
[source, cli]
----
network fcp adapter modify -node <node_name> -adapter <adapter_port> -topology direct
----
. 將目標連接埠的纜線直接連接至主機啟動器。
. 建立單一 LIF 介面：
+
[source, cli]
----
network interface create -vserver <SVM_name> -lif <lif_name> -data protocol fcp|fcp-nvme -home-node <node_name> -home-port <port_name>
----
+
LIF 建立完成後，連接埠會自動啟用。

. 確認連接埠已設定為直接連接：
+
[source, cli]
----
network fcp interface show
----
+
拓撲應顯示為直接。



--
====
.相關資訊
* 了解更多 link:../san-config/configure-fc-nvme-hosts-ha-pairs-reference.html["FC 和 FC-NVMe 架構以及直接連接組態"]。




== 設定 FC 介面卡速度

您應該設定介面卡目標連接埠速度，使其符合所連接裝置的速度，而非使用自動協商。設定為自動協商的連接埠、在接管/恢復或其他中斷之後、重新連線可能需要較長的時間。

.關於這項工作
由於此工作涵蓋叢集中的所有儲存虛擬機器（ SVM ）和所有生命、因此您必須使用 `-home-port` 和 `-home-lif` 限制此作業範圍的參數。如果不使用這些參數、則此作業會套用至叢集中的所有l生命、這可能是不理想的。

.開始之前
使用此介面卡做為其主連接埠的所有LIF都必須離線。

.步驟
. 將此介面卡上的所有生命段離線：
+
[source, cli]
----
network interface modify -vserver * -lif * { -home-node node1 -home-port 0c } -status-admin down
----
. 使介面卡離線：
+
[source, cli]
----
network fcp adapter modify -node node1 -adapter 0c -state down
----
+
如果介面卡未離線、您也可以從系統上適當的介面卡連接埠拔下纜線。

. 判斷連接埠介面卡的最大速度：
+
[source, cli]
----
fcp adapter show -instance
----
+
您無法修改介面卡速度超過最大速度。

. 變更介面卡速度：
+
[source, cli]
----
network fcp adapter modify -node node1 -adapter 0c -speed 16
----
. 將介面卡上線：
+
[source, cli]
----
network fcp adapter modify -node node1 -adapter 0c -state up
----
. 將介面卡上的所有生命項目上線：
+
[source, cli]
----
network interface modify -vserver * -lif * { -home-node node1 -home-port 0c } -status-admin up
----


.相關資訊
* 了解更多 link:limit-lun-access-portsets-igroups-concept.html["使用連接埠集和啟動器群組限制 LUN 存取的方法"]。
* 了解 link:selective-lun-map-concept.html#determine-whether-slm-is-enabled-on-a-lun-map["選擇性 LUN 對應（ SLM ）"] 如何限制用於存取 HA 配對所擁有之 LUN 的路徑。

